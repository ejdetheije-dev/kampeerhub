services:
  app:
    build:
      context: ..
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    environment:
      - LLM_MOCK=true
      - OPENROUTER_API_KEY=test-key
      - DATABASE_PATH=/app/database/test.db
    tmpfs:
      - /app/database
