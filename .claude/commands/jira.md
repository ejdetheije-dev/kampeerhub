Fetch Jira issue $ARGUMENTS and implement it.

Run this command to fetch and parse the issue:

```bash
node .claude/fetch-jira.js $ARGUMENTS
```

Use the output to understand the issue. Then implement what it describes. Follow the project conventions in CLAUDE.md.
